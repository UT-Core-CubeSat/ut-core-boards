(version 1)

(rule "IC1_reduced_clearance"
  (constraint clearance (min 0.10mm))
  (condition "A.insideCourtyard('IC1') || B.insideCourtyard('IC1')")
)

(rule "IC1_reduced_width"
  (constraint track_width (min 0.10mm))
  (condition "A.insideCourtyard('IC1')")
)
